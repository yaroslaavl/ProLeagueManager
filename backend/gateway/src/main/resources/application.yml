spring:
  cloud:
    consul:
      host: host.docker.internal
      port: 8500
      discovery:
        enabled: true
      config:
        enabled: true
        format: yaml
  config:
    import: 'consul:'
  application:
    name: gateway